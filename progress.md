## Прогресс

- Подготовка к деплою на Render.com:
  - `package.json` перенесён из `server/` в корень проекта, добавлен скрипт запуска `"start": "node server/server.js"`.
  - В `server/server.js` порт переведён на `process.env.PORT || 3000`, хост задан `0.0.0.0`.
  - В `server/server.js` ключ Groq убран из кода и берётся из `process.env.GROQ_API_KEY`.
  - В корень добавлен `.gitignore` с игнором `node_modules`.

- Создана папка `server`.
- Добавлен `server/package.json` с зависимостями `fastify` и `socket.io`.
- Добавлен `server/server.js` с базовым каркасом:
  - Fastify-сервер на порту 3000.
  - Socket.io с CORS `origin: "*"` для всех.
  - Обработка `find_match` с очередью ожидания и созданием комнаты.
  - Лог-заглушка старта таймера на 120 секунд после создания комнаты.
- Добавлена базовая клиентская часть в `client/`:
  - `index.html` с контейнером игры, подключением Socket.io CDN, `style.css` и `script.js`.
  - `style.css` с темной адаптивной темой и оформлением кнопок.
  - `script.js` с автозапросом `find_match`, статусом поиска и обработкой `match_found`.
- Добавлен базовый чат между двумя игроками:
  - В `server/server.js` добавлено событие `send_msg` и рассылка `new_msg` в комнате.
  - В `client/index.html` добавлена скрытая разметка чата (`chat-container`, `messages`, `input-area`).
  - В `client/style.css` добавлены стили чата, сообщений и поля ввода.
  - В `client/script.js` добавлены логика показа чата после `match_found`, отправка сообщений и рендер "Вы/Собеседник".
- Добавлены таймер и голосование:
  - В `server/server.js` добавлен серверный таймер 120 секунд с событиями `timer` и `time_up`.
  - В `server/server.js` добавлена обработка `vote` и персональная отправка `vote_result`.
  - В `server/server.js` добавлена обработка `opponent_disconnected` при выходе игрока из комнаты.
  - В `client/script.js` добавлены отображение таймера, блокировка чата по `time_up`, UI голосования и вывод результата.
  - В `client/index.html` и `client/style.css` добавлены элементы и стили таймера/голосования.
- Уточнена логика результата и UI:
  - В `server/server.js` `vote_result` теперь отправляет `yourVote`, `correctAnswer` и `isCorrect` без `opponentVote`.
  - В `client/script.js` результат голосования показывает только факт угадывания и что соперник — человек.
  - В `client/style.css` добавлены классы `result-win`/`result-lose` и стили тонкого скроллбара для `#messages`.
- Серьезно улучшен UX клиентской части (только `client/`):
  - Добавлен стартовый экран с кнопкой `Играть`, слоганом и плавным появлением.
  - Добавлен анимированный индикатор поиска соперника и улучшена визуализация состояния ожидания.
  - Улучшены кнопки голосования до карточек с иконками и hover-эффектами.
  - Добавлена красивая карточка результата с иконкой, цветовым состоянием и кнопкой `Играть снова`.
  - Добавлены анимации сообщений в чате и пульсирующий красный таймер в последние 10 секунд.
- Добавлены комплексные доработки чата и управления матчем:
  - В `server/server.js` добавлены `typing`, `opponent_typing`, `leave_room` и возвращено поле `opponentVote` в `vote_result`.
  - В `client/script.js` добавлены системные сообщения (`match_found`, 60/30 секунд, `opponent_disconnected`).
  - В `client/script.js` добавлен индикатор «Собеседник печатает...» с ограничением отправки `typing` (1 раз в секунду) и авто-скрытием.
  - В `client/index.html` и `client/style.css` добавлены меню `⋮`, действия «Завершить чат/К началу/Пожаловаться», модалка жалобы и подтверждение отправки.
  - В `client/index.html`, `client/style.css`, `client/script.js` добавлены минималистичные аватары сообщений и нейтральная строка о голосе соперника в карточке результата.
- Исправлены баги и добавлены новые механики:
  - В `client/script.js` кнопки `Завершить чат` и `К началу` блокируются до 4 сообщений, а у `Завершить чат` показан счетчик `(N/4)`.
  - В `server/server.js` добавлен `force_vote`: досрочный переход обоих игроков в фазу голосования через `time_up`.
  - В `server/server.js` и `client/script.js` доработан `leave_room`: соперник получает причину `left_menu` и видит сообщение про выход в главное меню.
  - В `server/server.js`/`client/script.js` исправлено отображение `opponentVote` в `vote_result`.
  - В `server/server.js`/`client/script.js` добавлен пошаговый чат с очередностью хода (`your_turn`/`opponent_turn`) и проверкой `send_msg`/`typing`.
  - Полностью обновлен визуальный стиль `client/style.css` в темно-серой палитре с неоново-зеленым акцентом и мобильной адаптацией.
- Внесены точечные правки логики первого хода и досрочного завершения:
  - В `server/server.js` первый ход теперь назначается случайно при `match_found` (50/50), затем игрокам отправляются `your_turn` и `opponent_turn`.
  - В `client/script.js` до получения команды от сервера поле ввода заблокировано у обоих, кнопка отправки скрыта у неактивного игрока, при ожидании показываются `Ожидайте...` и индикатор `Собеседник печатает...`.
  - В `server/server.js` перед `force_vote` второму игроку отправляется `chat_ended_early` с текстом `Собеседник завершил чат досрочно`.
  - В `client/script.js` на `chat_ended_early` добавляется системное сообщение в чат и сразу показывается блок голосования.
- Критическое исправление очередности хода:
  - В `server/server.js` в `send_msg` добавлена жесткая проверка `roomState.turn !== socket.id` с отклонением через `message_rejected` и текстом `Сейчас не ваша очередь`.
  - В `server/server.js` зафиксирован порядок старта комнаты: сначала создается `turn`, затем сразу рассылаются `your_turn`/`opponent_turn`.
  - В `client/script.js` подтверждена строгая блокировка ввода до `your_turn`, а на `message_rejected` показана всплывающая подсказка на 2 секунды.
- Исправлена рассылка `chat_ended_early` при `force_vote`:
  - В `server/server.js` перед голосованием событие `chat_ended_early` отправляется обоим игрокам.
  - Инициатор получает текст `Вы завершили чат досрочно.`, соперник — `Собеседник завершил чат досрочно.`.
  - Клиентский обработчик `chat_ended_early` в `client/script.js` уже выводит системное сообщение и затем показывает голосование.
- Добавлена раздача статических файлов клиента:
  - В `server/server.js` подключен `path` и зарегистрирован `@fastify/static` для папки `client` с `prefix: "/"`.
  - Теперь игру можно открывать по адресу `http://IP:3000` без ручного копирования файлов.
- Выполнен полный редизайн клиентского интерфейса в стиле Modern Dark UI + Neo-Glassmorphism (без изменений игровой логики):
  - В `client/index.html` полностью переработан стартовый экран: toolbar с иконками, инфоблок, карточки выбора режима, языковой селектор-заглушка и новая CTA-кнопка.
  - В `client/style.css` внедрена mobile-first визуальная система на CSS-переменных: новый темный glassmorphism-стиль для стартового экрана, чата, таймера, голосования, меню и модалки жалобы.
  - В `client/script.js` добавлен Canvas-фон с падающими геометрическими частицами (круги/треугольники/линии), параллаксом по размеру и мягкой глубиной через blur/прозрачность.
  - В `client/script.js` добавлено визуальное переключение карточек режимов (`Безопасный чат` / `Свободный чат`) без влияния на существующие события Socket.io, матчмейкинг, таймер и голосование.
- Внесены точечные правки UI без изменений игровой логики:
  - В `client/index.html` удалены кнопки `🚫` и `🎮` из верхней панели, добавлена кнопка `Отмена` на экран поиска и добавлен id `report-modal` для карточки модалки.
  - В `client/style.css` добавлены полупрозрачные фоны и `backdrop-filter` для `#start-screen`, `#chat-container`, `#game-screen`, `#chatMenu`, `#report-modal`, `#result-card`.
  - В `client/style.css` для неактивных пунктов меню (`Завершить чат`, `К началу`) оставлен обычный фон и установлен серый текст + `not-allowed`.
  - В `client/style.css` кнопка `Отправить` выровнена по центру через `display: flex; align-items: center; justify-content: center;`.
  - В `client/script.js` добавлена логика отмены поиска: по кнопке `Отмена` выполняются `resetMatchUi()` и `showStartScreen()`.
- Выполнено обновление иконок и исправление UI-багов без изменения игровой логики:
  - В `client/index.html` эмодзи `🔊`, `📶`, `🕐`, `🛡️`, `🧑`, `🤖` заменены на inline SVG с `stroke="currentColor"`.
  - В `client/style.css` добавлены состояния иконок: активные (`#4ADE80` + лёгкое свечение) и неактивные (`#555` без свечения), а также цвет щита для режимов (`Безопасный` — `#4ADE80`, `Свободный` — `#F5C542`).
  - В `client/script.js` исправлен возврат в главное меню после `Начать игру -> Отмена`: `showStartScreen()` возвращает `display: flex` и сбрасывает инлайн-сдвиги экранов.
  - В `client/style.css` унифицирована полупрозрачность для `#start-screen`, `#chat-container`, `#game-screen`, `#result-card`, `#report-modal`, `#chatMenu` (`rgba(26, 26, 26, 0.85)` + `backdrop-filter: blur(6px)`).
  - В `client/index.html`/`client/style.css` добавлен контейнер `.typing-dots` и анимация `dotBounce` с задержками `0s`, `0.2s`, `0.4s` для индикатора «Собеседник печатает».
- Выполнены дополнительные точечные правки UI (без изменения игровой логики):
  - В `client/style.css` для `#messages` настроен серый скроллбар под дизайн: WebKit (`width: 5px`, серый `thumb`, прозрачный `track`) и Firefox (`scrollbar-width: thin`, `scrollbar-color: #555 transparent`).
  - В `client/style.css` иконки человека/робота в `#result-icon` переведены в активный зелёный цвет `#4ADE80` с неоновым свечением.
  - В `client/style.css` увеличены SVG-иконки верхней панели (часы, динамик, антенна) до ~29px и немного увеличена SVG-иконка в карточке результата; иконки голосования и щитов режимов не изменялись.
  - В `client/index.html` удалена иконка ноты `🎵` из верхней панели.
  - В `client/index.html`/`client/script.js` добавлены переключатели `historyToggleBtn` и `soundToggleBtn` с визуальным toggle активного/неактивного состояния (зелёный со свечением ↔ серый без свечения) по клику.
- В `client/index.html` и `client/style.css` обновлена иконка главного меню:
  - В блоке `#start-screen` старая иконка заменена на новую крупную SVG из задачи.
  - Добавлен стиль `.start-logo-svg`: неоновый зелёный вид (`stroke #4ADE80` + `drop-shadow`), ограничение размера до ~190px и центрирование по горизонтали.
- Доработан контур логотипа в главном меню:
  - В `client/index.html` для группы путей логотипа увеличена толщина обводки до `stroke-width="35"`.
  - Добавлены сглаживающие параметры `stroke-linecap="round"` и `stroke-linejoin="round"` для более ровных и плавных линий.
  - Лишний короткий path с `v.01` внутри логотипа отсутствует (в текущем логотипе таких путей нет), поэтому удаление не потребовалось.
- В `client/index.html` в SVG логотипа главного меню вручную добавлена вертикальная линия антенны:
  - Перед закрывающим `</g>` добавлен `<line x1="246" y1="30" x2="246" y2="150" ...>`, чтобы восстановить видимую стойку антенны при сохранении существующего кружка на конце.
- Уточнены координаты линии антенны в SVG логотипе:
  - Линия перенесена в систему координат большой группы логотипа и выровнена по центру кружка антенны (`x1/x2=4304`, `y1=4456`, `y2=4302`), чтобы стойка антенны попадала точно к «шарику» и голове робота.
- Подстроен масштаб и акцент логотипа в главном меню:
  - В `client/index.html` у группы логотипа увеличена толщина линий до `stroke-width="45"`.
  - В `client/style.css` увеличен размер `.start-logo-svg` до `width: min(100%, 240px)` и логотип поднят выше через `margin: -20px auto 16px`.
- Добавлен Gemini-бот, который имитирует человека в чат-матчах:
  - В `server/package.json` добавлена зависимость `@google/generative-ai` и выполнен `npm install` в папке `server`.
  - В `server/server.js` добавлены конфигурация Gemini (`GoogleGenerativeAI`, модель `gemini-2.0-flash`), системный промпт бота и флаги `BOTS_ENABLED`/`BOT_CHANCE`.
  - В `find_match` расширено состояние комнаты: `isBotGame`, `botPlayerId`, `chatHistory`; добавлен выбор bot-матча по вероятности.
  - В `send_msg` добавлен триггер хода бота после `emitTurnState`, когда очередь перешла к `botPlayerId`.
  - Добавлена функция `handleBotTurn(roomId, userMessage)` с задержкой печати, отправкой `opponent_typing`, генерацией ответа через Gemini, обновлением истории и fallback-сообщением при ошибке.
- Исправлен старт хода в bot-матчах:
  - В `server/server.js` внутри `find_match` перед `emitTurnState(roomId)` добавлен явный сдвиг первого хода на реального игрока (если `isBotGame === true`), чтобы клиент сразу получал доступ к вводу и не зависал на стадии поиска.
- Обновлена логика поиска матча при пустой очереди:
  - В `server/server.js` в `find_match` добавлено немедленное создание комнаты с ботом (если `BOTS_ENABLED`), без ожидания второго живого игрока.
  - Для такого матча создаётся `botId`, игроку сразу отправляются `match_found` и `emitTurnState`, таймер запускается сразу.
  - Ветка с классической постановкой в очередь сохранена для случая, когда боты выключены.
- Улучшена диагностика и поведение Gemini-бота:
  - В `server/server.js` модель переключена на `gemini-1.5-flash` для более стабильной доступности.
  - В `handleBotTurn` увеличена задержка имитации печати до 2-5 секунд (`2000 + Math.random() * 3000`).
  - В `handleBotTurn` расширен `catch` с подробным логированием (`message`, `status`, полная ошибка и `error.response` при наличии).
  - В истории `startChat` убран стартовый `model`-элемент «Понял...», чтобы история начиналась с `user`-сообщения системного промпта.
- Добавлена защита от пустых ответов Gemini:
  - В `handleBotTurn` после получения `botResponse` добавлены логи текста и длины ответа.
  - Перед отправкой `new_msg` введена проверка: если ответ пустой/из пробелов, отправляется fallback `ой, связь тупит ))`.
- Выполнена миграция бота с Google Gemini на OpenRouter (Qwen):
  - В `server/package.json` удалена зависимость `@google/generative-ai`, добавлена `openai`.
  - В `server/server.js` удалён Gemini-блок и добавлена конфигурация `OpenAI` с `baseURL` OpenRouter и моделью `qwen/qwen3.6-plus-preview`.
  - В `handleBotTurn` блок `try/catch` переписан под `openai.chat.completions.create`, включая формат `messages` (`system/user/assistant`), ограничение `max_tokens`, `temperature`, сохранение истории и fallback при ошибке.
  - В папке `server` выполнен `npm install` для установки новой зависимости.
- Выполнена миграция с OpenRouter на Groq API:
  - В `server/server.js` заменён конфиг клиента `OpenAI`: `baseURL` переключён на `https://api.groq.com/openai/v1`, обновлён `apiKey`.
  - Модель бота изменена на `llama-3.1-8b-instant` через константу `BOT_MODEL`.
  - Содержимое `BOT_SYSTEM_PROMPT` приведено к новой версии из задачи (короткий стиль общения подростка без лишних примеров).
  - Логика `handleBotTurn` сохранена и продолжает использовать `BOT_MODEL` в `openai.chat.completions.create`.
